#include <string>
#include <stdio.h>
#include <vector>

using namespace std;

class Logger {

public:
    Logger();

    void log(int timestamp, const string& message) {

    }

    // Return the logs starting from a given timestamp
    vector<string> getLogsSince(int timestamp) {

    }
};